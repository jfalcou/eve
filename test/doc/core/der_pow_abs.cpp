#include <eve/function/derivative/pow_abs.hpp>
#include <eve/wide.hpp>
#include <iostream>

using wide_ft = eve::wide <float, eve::fixed<4>>;

int main()
{
  wide_ft pf = { 0.0f, 1.0f, -1.0f, -0.5f};

  std::cout
    << "---- simd" << '\n'
    << "<- pf                   = " << pf << '\n'
    << "-> derivative(pow_abs)(pf) = " << eve::derivative(eve::pow_abs)(pf) << '\n';

  float xf = 1.0f;

  std::cout
    << "---- scalar"  << '\n'
    << "<- xf                   = " << xf << '\n'
    << "-> derivative(pow_abs)(xf) = " << eve::derivative(eve::pow_abs)(xf) << '\n'; 
  return 0;
}
