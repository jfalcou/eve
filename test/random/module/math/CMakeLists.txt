##==================================================================================================
##  EVE - Expressive Vector Engine
##  Copyright 2019 Joel FALCOU
##  Copyright 2019 Jean-Thierry Lapreste
##
##  Licensed under the MIT License <http://opensource.org/licenses/MIT>.
##  SPDX-License-Identifier: MIT
##==================================================================================================

##==================================================================================================
## Setup aggregation of tests
##==================================================================================================
add_custom_target(random.math.exe)
add_custom_target(random.math.scalar.exe)
add_custom_target(random.math.simd.exe)
add_dependencies(random.math.exe random.math.scalar.exe)
add_dependencies(random.math.exe random.math.simd.exe)
add_dependencies(random.scalar.exe random.math.scalar.exe)
add_dependencies(random.simd.exe random.math.simd.exe)
add_dependencies(random.exe random.math.exe)

##==================================================================================================
## Elementwise function unit tests
##==================================================================================================
make_all_units(ROOT random.math NAME acos                  ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME acosd                 ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME acosh                 ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME acospi                ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME acot                  ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME acotd                 ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME acoth                 ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME acotpi                ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME acsc                  ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME acscd                 ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME acsch                 ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME acscpi                ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME asec                  ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME asecd                 ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME asech                 ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME asecpi                ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME asin                  ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME asind                 ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME asinh                 ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME asinpi                ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME atan                  ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME atand                 ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME atanh                 ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME atanpi                ARCH scalar simd TYPES ${real_types}    )
##make_all_units(ROOT random.math NAME atan2                 ARCH scalar simd TYPES ${real_types}    )
##make_all_units(ROOT random.math NAME atan2d                ARCH scalar simd TYPES ${real_types}    )
##make_all_units(ROOT random.math NAME atan2pi               ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME cos                   ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME cosd                  ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME cospi                 ARCH scalar simd TYPES ${signed_types}  )
make_all_units(ROOT random.math NAME cosh                  ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME cot                   ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME cotd                  ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME coth                  ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME cotpi                 ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME csc                   ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME cscd                  ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME csch                  ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME cscpi                 ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME deginrad              ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME exp                   ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME exp10                 ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME exp2                  ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME expm1                 ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME iceil                 ARCH scalar simd TYPES ${all_types}     )
make_all_units(ROOT random.math NAME log                   ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME log10                 ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME log1p                 ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME log2                  ARCH scalar simd TYPES ${real_types}    )
### TEMPORARY MEASURE
### make_all_units(ROOT random.math NAME pow                   ARCH scalar simd TYPES ${all_types}    )
make_all_units(ROOT random.math NAME pow_abs               ARCH scalar simd TYPES ${real_types}    )
### TEMPORARY MEASURE
make_all_units(ROOT random.math NAME radindeg              ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME radinpi               ARCH scalar simd TYPES ${real_types}    )
##make_all_units(ROOT random.math NAME rempio2               ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME sec                   ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME secd                  ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME sech                  ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME secpi                 ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME sin                   ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME sincos                ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME sind                  ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME sindcosd              ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME sinh                  ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME sinhcosh              ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME sinpi                 ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME sinpicospi            ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME tan                   ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME tand                  ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME tanh                  ARCH scalar simd TYPES ${real_types}    )
make_all_units(ROOT random.math NAME tanpi                 ARCH scalar simd TYPES ${real_types}    )
