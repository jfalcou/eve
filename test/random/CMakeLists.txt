##==================================================================================================
##  EVE - Expressive Vector Engine
##  Copyright 2018 Joel FALCOU
##
##  Licensed under the MIT License <http://opensource.org/licenses/MIT>.
##  SPDX-License-Identifier: MIT
##==================================================================================================

##==================================================================================================
## General random tests
make_all_units(ROOT random NAME abs               ARCH scalar simd TYPES ${all_types}       CARDINAL -1)  
make_all_units(ROOT random NAME acos              ARCH scalar simd TYPES ${real_types}      CARDINAL -1)
make_all_units(ROOT random NAME acosd             ARCH scalar simd TYPES ${real_types}      CARDINAL -1)
make_all_units(ROOT random NAME acosh             ARCH scalar simd TYPES ${real_types}      CARDINAL -1)
make_all_units(ROOT random NAME acospi            ARCH scalar simd TYPES ${real_types}      CARDINAL -1)
make_all_units(ROOT random NAME acot              ARCH scalar simd TYPES ${real_types}      CARDINAL -1)
make_all_units(ROOT random NAME acotd             ARCH scalar simd TYPES ${real_types}      CARDINAL -1)  
make_all_units(ROOT random NAME acoth             ARCH scalar simd TYPES ${real_types}      CARDINAL -1)
make_all_units(ROOT random NAME acotpi            ARCH scalar simd TYPES ${real_types}      CARDINAL -1)
make_all_units(ROOT random NAME acsc              ARCH scalar simd TYPES ${real_types}      CARDINAL -1)
make_all_units(ROOT random NAME acscd             ARCH scalar simd TYPES ${real_types}      CARDINAL -1)
make_all_units(ROOT random NAME acscpi            ARCH scalar simd TYPES ${real_types}      CARDINAL -1)
make_all_units(ROOT random NAME arg               ARCH scalar simd TYPES ${real_types}      CARDINAL -1)    
make_all_units(ROOT random NAME asec              ARCH scalar simd TYPES ${real_types}      CARDINAL -1)
make_all_units(ROOT random NAME asecd             ARCH scalar simd TYPES ${real_types}      CARDINAL -1)
make_all_units(ROOT random NAME asecpi            ARCH scalar simd TYPES ${real_types}      CARDINAL -1)
make_all_units(ROOT random NAME asin              ARCH scalar simd TYPES ${real_types}      CARDINAL -1)
make_all_units(ROOT random NAME asind             ARCH scalar simd TYPES ${real_types}      CARDINAL -1)
make_all_units(ROOT random NAME asinh             ARCH scalar simd TYPES ${real_types}      CARDINAL -1)    
make_all_units(ROOT random NAME asinpi            ARCH scalar simd TYPES ${real_types}      CARDINAL -1)
make_all_units(ROOT random NAME atan              ARCH scalar simd TYPES ${real_types}      CARDINAL -1)
make_all_units(ROOT random NAME atand             ARCH scalar simd TYPES ${real_types}      CARDINAL -1)
make_all_units(ROOT random NAME atanh             ARCH scalar simd TYPES ${real_types}      CARDINAL -1)    
make_all_units(ROOT random NAME atanpi            ARCH scalar simd TYPES ${real_types}      CARDINAL -1)
make_all_units(ROOT random NAME bitofsign         ARCH scalar simd TYPES ${signed_types}    CARDINAL -1) 
make_all_units(ROOT random NAME bit_mask      ARCH scalar simd TYPES ${all_types}       CARDINAL -1)
make_all_units(ROOT random NAME bit_not       ARCH scalar simd TYPES ${integral_types}  CARDINAL -1) 
make_all_units(ROOT random NAME cbrt              ARCH scalar simd TYPES ${real_types}      CARDINAL -1)   
make_all_units(ROOT random NAME ceil              ARCH scalar simd TYPES ${all_types}       CARDINAL -1)
make_all_units(ROOT random NAME conj              ARCH scalar simd TYPES ${real_types}      CARDINAL -1) 
make_all_units(ROOT random NAME cosh              ARCH scalar simd TYPES ${real_types}      CARDINAL -1)   
make_all_units(ROOT random NAME csch              ARCH scalar simd TYPES ${real_types}      CARDINAL -1) 
make_all_units(ROOT random NAME exp               ARCH scalar simd TYPES ${real_types}      CARDINAL -1)   
make_all_units(ROOT random NAME exp10             ARCH scalar simd TYPES ${real_types}      CARDINAL -1)   
make_all_units(ROOT random NAME exp2              ARCH scalar simd TYPES ${real_types}      CARDINAL -1)   
make_all_units(ROOT random NAME expm1             ARCH scalar simd TYPES ${real_types}      CARDINAL -1)    
make_all_units(ROOT random NAME exponent          ARCH scalar simd TYPES ${real_types}      CARDINAL -1) 
##make_all_units(ROOT random NAME exponentbits      ARCH scalar simd TYPES ${real_types}      CARDINAL -1)    
make_all_units(ROOT random NAME floor              ARCH scalar simd TYPES ${all_types}       CARDINAL -1) 
make_all_units(ROOT random NAME frac              ARCH scalar simd TYPES ${all_types}       CARDINAL -1) 
make_all_units(ROOT random NAME iceil             ARCH scalar simd TYPES ${all_types}       CARDINAL -1)
make_all_units(ROOT random NAME ifloor            ARCH scalar simd TYPES ${all_types}       CARDINAL -1)  
##make_all_units(ROOT random NAME ifrexp            ARCH scalar simd TYPES ${real_types}      CARDINAL -1)    
make_all_units(ROOT random NAME inc               ARCH scalar simd TYPES${all_types}        CARDINAL -1)  
make_all_units(ROOT random NAME indeg             ARCH scalar simd TYPES${all_types}        CARDINAL -1)    
make_all_units(ROOT random NAME inearest          ARCH scalar simd TYPES ${all_types}       CARDINAL -1)  
make_all_units(ROOT random NAME inpi              ARCH scalar simd TYPES${all_types}        CARDINAL -1)    
make_all_units(ROOT random NAME iround            ARCH scalar simd TYPES ${all_types}       CARDINAL -1)   
make_all_units(ROOT random NAME is_denormal       ARCH scalar simd TYPES ${all_types}       CARDINAL -1)  
make_all_units(ROOT random NAME is_eqz            ARCH scalar simd TYPES ${all_types}       CARDINAL -1) 
make_all_units(ROOT random NAME is_even           ARCH scalar simd TYPES ${all_types}       CARDINAL -1) 
make_all_units(ROOT random NAME is_finite         ARCH scalar simd TYPES ${all_types}       CARDINAL -1)    
make_all_units(ROOT random NAME is_flint          ARCH scalar simd TYPES ${all_types}       CARDINAL -1)       
make_all_units(ROOT random NAME is_gez            ARCH scalar simd TYPES ${all_types}       CARDINAL -1)  
make_all_units(ROOT random NAME is_gtz            ARCH scalar simd TYPES ${all_types}       CARDINAL -1)      
make_all_units(ROOT random NAME is_imag           ARCH scalar simd TYPES ${real_types}      CARDINAL -1)     
make_all_units(ROOT random NAME is_infinite       ARCH scalar simd TYPES ${all_types}       CARDINAL -1)   
make_all_units(ROOT random NAME is_lez            ARCH scalar simd TYPES ${all_types}       CARDINAL -1)    
make_all_units(ROOT random NAME is_ltz            ARCH scalar simd TYPES ${all_types}       CARDINAL -1)  
make_all_units(ROOT random NAME is_nan            ARCH scalar simd TYPES ${all_types}       CARDINAL -1)    
make_all_units(ROOT random NAME is_negative       ARCH scalar simd TYPES ${all_types}       CARDINAL -1)
make_all_units(ROOT random NAME is_nez            ARCH scalar simd TYPES ${all_types}       CARDINAL -1)  
make_all_units(ROOT random NAME is_ngez           ARCH scalar simd TYPES ${all_types}       CARDINAL -1)     
make_all_units(ROOT random NAME is_ngtz           ARCH scalar simd TYPES ${all_types}       CARDINAL -1)    
make_all_units(ROOT random NAME is_nlez           ARCH scalar simd TYPES ${all_types}       CARDINAL -1)    
make_all_units(ROOT random NAME is_nltz           ARCH scalar simd TYPES ${all_types}       CARDINAL -1)  
make_all_units(ROOT random NAME is_normal         ARCH scalar simd TYPES ${all_types}       CARDINAL -1)     
make_all_units(ROOT random NAME is_not_denormal   ARCH scalar simd TYPES ${all_types}       CARDINAL -1)          
make_all_units(ROOT random NAME is_not_finite     ARCH scalar simd TYPES ${all_types}       CARDINAL -1)    
make_all_units(ROOT random NAME is_not_imag       ARCH scalar simd TYPES ${all_types}       CARDINAL -1)      
make_all_units(ROOT random NAME is_not_inf        ARCH scalar simd TYPES ${all_types}       CARDINAL -1)     
make_all_units(ROOT random NAME is_not_nan        ARCH scalar simd TYPES ${all_types}       CARDINAL -1)  
make_all_units(ROOT random NAME is_not_real       ARCH scalar simd TYPES ${all_types}       CARDINAL -1)      
make_all_units(ROOT random NAME is_odd            ARCH scalar simd TYPES ${all_types}       CARDINAL -1) 
make_all_units(ROOT random NAME is_positive       ARCH scalar simd TYPES ${all_types}       CARDINAL -1)  
make_all_units(ROOT random NAME is_real           ARCH scalar simd TYPES ${real_types}      CARDINAL -1) 
make_all_units(ROOT random NAME itrunc            ARCH scalar simd TYPES ${all_types}       CARDINAL -1)   
make_all_units(ROOT random NAME log               ARCH scalar simd TYPES ${real_types}      CARDINAL -1)   
make_all_units(ROOT random NAME log10             ARCH scalar simd TYPES ${real_types}      CARDINAL -1)   
make_all_units(ROOT random NAME log1p             ARCH scalar simd TYPES ${real_types}      CARDINAL -1)   
make_all_units(ROOT random NAME log2              ARCH scalar simd TYPES ${real_types}      CARDINAL -1)   
make_all_units(ROOT random NAME mantissa          ARCH scalar simd TYPES ${real_types}      CARDINAL -1) 
make_all_units(ROOT random NAME nearest           ARCH scalar simd TYPES ${all_types}       CARDINAL -1)   
make_all_units(ROOT random NAME next              ARCH scalar simd TYPES ${all_types}       CARDINAL -1) 
make_all_units(ROOT random NAME oneminus          ARCH scalar simd TYPES ${all_types}       CARDINAL -1)     
make_all_units(ROOT random NAME prev              ARCH scalar simd TYPES ${all_types}       CARDINAL -1)
make_all_units(ROOT random NAME rec               ARCH scalar simd TYPES ${real_types}      CARDINAL -1)  
make_all_units(ROOT random NAME round             ARCH scalar simd TYPES ${all_types}       CARDINAL -1)
make_all_units(ROOT random NAME rsqrt             ARCH scalar simd TYPES ${real_types}      CARDINAL -1)  
make_all_units(ROOT random NAME sech              ARCH scalar simd TYPES ${real_types}      CARDINAL -1)  
make_all_units(ROOT random NAME sign              ARCH scalar simd TYPES ${all_types}       CARDINAL -1)  
make_all_units(ROOT random NAME signz             ARCH scalar simd TYPES ${all_types}       CARDINAL -1)  
make_all_units(ROOT random NAME sinh              ARCH scalar simd TYPES ${real_types}      CARDINAL -1)  
make_all_units(ROOT random NAME sqr               ARCH scalar simd TYPES ${real_types}      CARDINAL -1)  
make_all_units(ROOT random NAME sqr_abs           ARCH scalar simd TYPES ${real_types}      CARDINAL -1)  
make_all_units(ROOT random NAME sqrt              ARCH scalar simd TYPES ${real_types}      CARDINAL -1)  
make_all_units(ROOT random NAME tanh              ARCH scalar simd TYPES ${real_types}      CARDINAL -1)
make_all_units(ROOT random NAME trunc             ARCH scalar simd TYPES ${all_types}       CARDINAL -1)
make_all_units(ROOT random NAME unary_minus       ARCH scalar simd TYPES ${real_types}      CARDINAL -1)
make_all_units(ROOT random NAME unary_plus        ARCH scalar simd TYPES ${all_types}       CARDINAL -1)
  