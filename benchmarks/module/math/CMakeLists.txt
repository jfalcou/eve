##==================================================================================================
##  EVE - Expressive Vector Engine
##  Copyright 2019 Joel FALCOU
##  Copyright 2019 Jean-Thierry Lapreste
##
##  Licensed under the MIT License <http://opensource.org/licenses/MIT>.
##  SPDX-License-Identifier: MIT
##==================================================================================================

##==================================================================================================
## Setup aggregation of tests
##==================================================================================================
add_custom_target(math.bench)

##==================================================================================================
## Elementwise function bench tests
##==================================================================================================
make_all_benchs(ROOT math NAME acos        TYPES ${real_types}    )
make_all_benchs(ROOT math NAME acosd       TYPES ${real_types}    )
make_all_benchs(ROOT math NAME acosh       TYPES ${real_types}    )
make_all_benchs(ROOT math NAME acospi      TYPES ${real_types}    )
make_all_benchs(ROOT math NAME acot        TYPES ${real_types}    )
make_all_benchs(ROOT math NAME acotd       TYPES ${real_types}    )
make_all_benchs(ROOT math NAME acoth       TYPES ${real_types}    )
make_all_benchs(ROOT math NAME acotpi      TYPES ${real_types}    )
make_all_benchs(ROOT math NAME acsc        TYPES ${real_types}    )
make_all_benchs(ROOT math NAME acscd       TYPES ${real_types}    )
make_all_benchs(ROOT math NAME acsch       TYPES ${real_types}    )
make_all_benchs(ROOT math NAME acscpi      TYPES ${real_types}    )
make_all_benchs(ROOT math NAME asec        TYPES ${real_types}    )
make_all_benchs(ROOT math NAME asecd       TYPES ${real_types}    )
make_all_benchs(ROOT math NAME asech       TYPES ${real_types}    )
make_all_benchs(ROOT math NAME asecpi      TYPES ${real_types}    )
make_all_benchs(ROOT math NAME asin        TYPES ${real_types}    )
make_all_benchs(ROOT math NAME asind       TYPES ${real_types}    )
make_all_benchs(ROOT math NAME asinh       TYPES ${real_types}    )
make_all_benchs(ROOT math NAME asinpi      TYPES ${real_types}    )
make_all_benchs(ROOT math NAME atan        TYPES ${real_types}    )
make_all_benchs(ROOT math NAME atand       TYPES ${real_types}    )
make_all_benchs(ROOT math NAME atanh       TYPES ${real_types}    )
make_all_benchs(ROOT math NAME atanpi      TYPES ${real_types}    )
make_all_benchs(ROOT math NAME atan2       TYPES ${real_types}    )
make_all_benchs(ROOT math NAME atan2d      TYPES ${real_types}    )
make_all_benchs(ROOT math NAME atan2pi     TYPES ${real_types}    )
make_all_benchs(ROOT math NAME cos         TYPES ${real_types}    )
make_all_benchs(ROOT math NAME cosd        TYPES ${real_types}    )
make_all_benchs(ROOT math NAME cospi       TYPES ${real_types}    )
make_all_benchs(ROOT math NAME cosh        TYPES ${real_types}    )
make_all_benchs(ROOT math NAME cot         TYPES ${real_types}    )
make_all_benchs(ROOT math NAME cotd        TYPES ${real_types}    )
make_all_benchs(ROOT math NAME coth        TYPES ${real_types}    )
make_all_benchs(ROOT math NAME cotpi       TYPES ${real_types}    )
make_all_benchs(ROOT math NAME csc         TYPES ${real_types}    )
make_all_benchs(ROOT math NAME cscd        TYPES ${real_types}    )
make_all_benchs(ROOT math NAME csch        TYPES ${real_types}    )
make_all_benchs(ROOT math NAME cscpi       TYPES ${real_types}    )
make_all_benchs(ROOT math NAME deginrad    TYPES ${real_types}    )
make_all_benchs(ROOT math NAME exp         TYPES ${real_types}    )
make_all_benchs(ROOT math NAME exp10       TYPES ${real_types}    )
make_all_benchs(ROOT math NAME exp2        TYPES ${real_types}    )
make_all_benchs(ROOT math NAME expm1       TYPES ${real_types}    )
make_all_benchs(ROOT math NAME log         TYPES ${real_types}    )
make_all_benchs(ROOT math NAME log10       TYPES ${real_types}    )
make_all_benchs(ROOT math NAME log1p       TYPES ${real_types}    )
make_all_benchs(ROOT math NAME log2        TYPES ${real_types}    )
make_all_benchs(ROOT math NAME pow         TYPES ${real_types}    )
make_all_benchs(ROOT math NAME pow_abs     TYPES ${real_types}    )
make_all_benchs(ROOT math NAME radindeg    TYPES ${real_types}    )
make_all_benchs(ROOT math NAME radinpi     TYPES ${real_types}    )
make_all_benchs(ROOT math NAME rempio2     TYPES ${real_types}    )
make_all_benchs(ROOT math NAME sec         TYPES ${real_types}    )
make_all_benchs(ROOT math NAME secd        TYPES ${real_types}    )
make_all_benchs(ROOT math NAME sech        TYPES ${real_types}    )
make_all_benchs(ROOT math NAME secpi       TYPES ${real_types}    )
make_all_benchs(ROOT math NAME sin         TYPES ${real_types}    )
make_all_benchs(ROOT math NAME sincos      TYPES ${real_types}    )
make_all_benchs(ROOT math NAME sind        TYPES ${real_types}    )
make_all_benchs(ROOT math NAME sindcosd    TYPES ${real_types}    )
make_all_benchs(ROOT math NAME sinh        TYPES ${real_types}    )
make_all_benchs(ROOT math NAME sinhcosh    TYPES ${real_types}    )
make_all_benchs(ROOT math NAME sinpi       TYPES ${real_types}    )
make_all_benchs(ROOT math NAME sinpicospi  TYPES ${real_types}    )
make_all_benchs(ROOT math NAME tan         TYPES ${real_types}    )
make_all_benchs(ROOT math NAME tand        TYPES ${real_types}    )
make_all_benchs(ROOT math NAME tanh        TYPES ${real_types}    )
make_all_benchs(ROOT math NAME tanpi       TYPES ${real_types}    )
