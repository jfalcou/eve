##==================================================================================================
##  EVE - Expressive Vector Engine
##  Copyright 2020 Joel FALCOU
##  Copyright 2020 Jean-Thierry LAPRESTE
##
##  Licensed under the MIT License <http://opensource.org/licenses/MIT>.
##  SPDX-License-Identifier: MIT
##==================================================================================================

##==================================================================================================
## Setup aggregation of tests
##==================================================================================================
add_custom_target(std.bench)
add_custom_target(std.scalar.bench)
add_custom_target(std.simd.bench)
add_dependencies(std.bench std.scalar.bench)
add_dependencies(std.bench std.simd.bench)
add_dependencies(bench std.bench)

##==================================================================================================
## List of tests
##==================================================================================================
make_all_benchs(ROOT std NAME abs                  ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME acos                 ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME acosd                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME acosh                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME acospi               ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME acot                 ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME acotd                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME acoth                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME acotpi               ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME acsc                 ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME acscd                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME acsch                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME acscpi               ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME add                  ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME arg                  ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME asec                 ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME asecd                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME asech                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME asecpi               ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME asin                 ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME asind                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME asinh                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME asinpi               ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME atan                 ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME atan2                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME atan2d               ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME atan2pi              ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME atand                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME atanh                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME atanpi               ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME average              ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME binarize             ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME binarize_not         ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME bit_and              ARCH scalar TYPES ${integral_types}  )
make_all_benchs(ROOT std NAME bit_andnot           ARCH scalar TYPES ${integral_types}  )
make_all_benchs(ROOT std NAME bit_ceil             ARCH scalar TYPES ${uint_types} )
make_all_benchs(ROOT std NAME bit_floor            ARCH scalar TYPES ${uint_types} )
make_all_benchs(ROOT std NAME bit_mask             ARCH scalar TYPES ${integral_types} )
make_all_benchs(ROOT std NAME bit_not              ARCH scalar TYPES ${integral_types} )
make_all_benchs(ROOT std NAME bit_notand           ARCH scalar TYPES ${integral_types} )
make_all_benchs(ROOT std NAME bit_notor            ARCH scalar TYPES ${integral_types} )
make_all_benchs(ROOT std NAME bitofsign            ARCH scalar TYPES ${integral_types} )
make_all_benchs(ROOT std NAME bit_or               ARCH scalar TYPES ${integral_types} )
make_all_benchs(ROOT std NAME bit_ornot            ARCH scalar TYPES ${integral_types} )
make_all_benchs(ROOT std NAME bit_select           ARCH scalar TYPES ${integral_types} )
make_all_benchs(ROOT std NAME bit_shl              ARCH scalar TYPES ${integral_types} )
make_all_benchs(ROOT std NAME bit_shr              ARCH scalar TYPES ${integral_types} )
make_all_benchs(ROOT std NAME bit_xor              ARCH scalar TYPES ${integral_types} )
make_all_benchs(ROOT std NAME cbrt                 ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME ceil                 ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME clamp                ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME copysign             ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME cos                  ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME cosd                 ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME cosh                 ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME cospi                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME cot                  ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME cotd                 ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME coth                 ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME cotpi                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME csc                  ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME cscd                 ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME csch                 ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME cscpi                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME dec                  ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME dist                 ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME div                  ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME exp                  ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME exp2                 ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME exp10                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME expm1                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME exponent             ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME fdim                 ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME firstbitset          ARCH scalar TYPES ${integral_types} )
make_all_benchs(ROOT std NAME firstbitunst         ARCH scalar TYPES ${integral_types} )
make_all_benchs(ROOT std NAME floor                ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME fma                  ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME fms                  ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME fnma                 ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME fnms                 ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME frac                 ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME frexp                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME hypot                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME iceil                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME ifloor               ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME inc                  ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME inearest             ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME iround               ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME is_denormal          ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME is_equal             ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_eqz               ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_even              ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_finite            ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_flint             ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME is_gez               ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_greater           ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_greater_equal     ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_gtz               ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_imag              ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_infinite          ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_less              ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_less_equal        ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_lessgreater       ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_lez               ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_ltz               ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_nan               ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_negative          ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_nez               ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_ngez              ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_ngtz              ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_nlez              ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_nltz              ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_normal            ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME is_not_denormal      ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME is_not_equal         ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME is_not_finite        ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME is_not_greater       ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME is_not_greater_equal ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME is_not_less          ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME is_not_less_equal    ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME is_not_nan           ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME is_not_real          ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME is_odd               ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_ordered           ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME is_positive          ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME is_pow2              ARCH scalar TYPES ${uint_types} )
make_all_benchs(ROOT std NAME is_real              ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME is_unordered         ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME itrunc               ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME ldexp                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME log                  ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME log1p                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME log10                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME log2                 ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME logical_and          ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME logical_andnot       ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME logical_not          ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME logical_notand       ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME logical_notor        ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME logical_ornot        ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME logical_xor          ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME manhattan            ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME mantissa             ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME max                  ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME maxmag               ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME min                  ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME minmag               ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME minus                ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME modf                 ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME mul                  ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME nearest              ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME negate               ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME negatenz             ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME next                 ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME nextafter            ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME oneminus             ARCH scalar TYPES ${all_types}  )
make_all_benchs(ROOT std NAME pow                  ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME pow_abs              ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME radindeg             ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME radinpi              ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME rem                  ARCH scalar TYPES ${integral_types} )
make_all_benchs(ROOT std NAME popcount             ARCH scalar TYPES ${uint_types} )
make_all_benchs(ROOT std NAME sin                  ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME sind                 ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME sinh                 ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME sinpi                ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME tan                  ARCH scalar TYPES ${real_types} )
make_all_benchs(ROOT std NAME tanh                 ARCH scalar TYPES ${real_types} )
